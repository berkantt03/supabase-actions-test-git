{
  "name": "supabase-migration-test",
  "version": "1.0.0",
  "description": "Coolify pre-deployment testi icin bos proje",
  "scripts": {
    "start": "npx -y supabase db push --db-url $SUPABASE_DB_URL && node -e \"setInterval(() => console.log('Uygulama ayakta bekliyor...'), 60000)\""
  }
}